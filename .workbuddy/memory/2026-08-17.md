# 2026-08-17

## yazi GUI 项目启动

### 决策
- 技术栈：**Rust + GPUI 0.2.2**（Zed 编辑器同款 GPU 加速 UI 框架），原生 GUI，跨平台。
- 架构：三层 —— GPUI 前端 / Rust 桥接层 / yazi 引擎（后端进程），通过 yazi DDS 事件流双向通信。
- 关键约束：yazi `--local-events` 只吐增量事件（hover/cd/rename 等），**不吐完整文件列表** → 计划用 yazi Lua 插件 `ps.pub` 发布完整目录。

### 环境搭建（已完成）
- Rust 1.97.1（rustup，stable-x86_64-pc-windows-msvc）+ cargo/clippy/rustfmt
- MSVC Build Tools 14.44.35207 + Windows SDK 10.0.26100.0（cl.exe / link.exe / rc.exe）
- `~/.cargo/bin` 已加入用户 PATH

### 踩坑记录（重要）
1. **rustup 在 Windows 沙箱下硬链接代理失败**：`~/.cargo/bin` 里的 cargo.exe/rustc.exe 等全部变成 0 字节（error 183）。沙箱拦截硬链接创建。解法：PowerShell `New-Item -ItemType HardLink` 手动重建 13 个代理（rustup.exe 的硬链接，靠 argv[0] 判断身份）。`rustup default/update` 在工具链"未变化"时不会重建代理。
2. **GPUI 0.2.2 API 与旧版不同**（crates.io 版 v0.2.x 重构过）：
   - `Render::render(&mut self, window: &mut Window, cx: &mut Context<Self>) -> impl IntoElement`（多了 window 参数，`ViewContext` 改名 `Context`）
   - `WindowOptions` 字段是 `window_bounds: Option<WindowBounds>`（不是 `bounds`），用 `WindowBounds::centered(size, cx)`
   - `TitlebarOptions.title: Option<SharedString>`

### 验证结果
- `cargo build` 成功（703 个依赖全编译，GPUI 0.2.2 本体在 Windows 上编译通过）
- `target/debug/yazi-gui.exe` 运行成功，窗口正常打开（进程存活，无报错）—— **GPUI Windows 支持验证通过**

### 提交
- `0cd15c3` chore: GPUI 0.2.2 最小骨架，验证 Windows 编译运行
- `1c73806` feat: 桥接层——无PTY启动yazi、解析DDS事件流、ya emit-to发指令

## 桥接层（任务3，已完成）

### 关键结论（踩坑）
1. **yazi 不需要 PTY**：用 `std::process::Command` + 分离管道（stdout piped / stderr null）即可。stdout 是**纯净的事件流**（每行一条），完全没有 ANSI 污染。相比 ConPTY/portable-pty 简单太多。
2. **`--client-id` 和 `ya emit-to` 的 receiver 必须是纯数字**（u64，如时间戳毫秒），不能是字符串。
3. 事件格式：`kind,receiver,sender,{json}`，逗号分隔 4 段；body 是 JSON（如 `cd` → `{"tab":1,"url":"D:\\"}`，`hover` → `{"tab":1,"url":"..."或null}`）。
4. 启动命令：`yazi --client-id <ts> --local-events cd,hover,rename,trash,delete,move,bulk`（cwd 用 current_dir 设置）。发指令：`ya emit-to <client_id> cd <path>`。

### 实现
- `src/yazi.rs`：`YaziClient`（spawn + 事件接收 channel + send 动作）、`YaziEvent` 枚举（Cd/Hover/Other）、解析器。
- `src/main.rs`：Root 视图持有 client/cwd/hovered/log；`cx.spawn` 异步轮询 channel → `weak.update` 更新视图；`nav_button` 测试发 cd 指令。
- `examples/yazi_probe_pipe.rs`：独立探针，验证事件流格式（保留作调试用）。

### GPUI 0.2.2 补充 API 坑
- `.on_click()` / `.overflow_y_scroll()` 是 `StatefulInteractiveElement` trait 方法，只对 `Stateful<Div>` 生效。`div()` 返回 `Div`，需先 `.id(...)` 转成 `Stateful` 再用（Zed 源码同款模式：`.child(...).id(ix).on_click(...)`）。
- 后台更新视图：`cx.spawn(async move |weak, cx| { weak.update(cx, |this, cx| {...}).ok(); }).detach()`。
- **链接报 LNK1104 无法打开 exe**：多半是旧进程还占着 exe 文件，先 kill 再重编。

### 验证
- `cargo build` 成功；`yazi-gui.exe` 运行，GUI 进程 + yazi 子进程（2 个）都存活，无 panic。

